<!doctype html>
<html lang="en">

    <%- include('../part/head') %>

    <body>
        <%- include('../part/nav') %>
            <div id="background"></div>
            <div class="container">
                <p>
                    <% if (user.verifyEmail) { %>
                        Email đã được xác thực rồi.
                    <% } else { %>
                        Vui lòng kiểm tra email <a href="#"><%=user.email%></a>  để xác thực tài khoản.
                    <% } %>
                </p>
            </div>
    </body>

<%- include('../part/foot') %>

</html>