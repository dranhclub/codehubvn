<!doctype html>
<html lang="en">

    <%- include('../part/head') %>

    <body>
        <%- include('../part/nav') %>
            <div id="background"></div>
            <div class="container">
                <div class="row">
                    <div class="col-xs-12 col-md-6">
                        <div style="color: #444; font-size: 20px">Tên</div>
                        <h2><%= user.name %></h2>
                        <div style="color: #444; font-size: 20px">Email</div>
                        <h2><%= user.email %></h2>
                        <div>
                            <a href="#" class="fantasy-btn">Sửa thông tin</a>
                            <a href="/logout" class="fantasy-btn">Đăng xuất</a>
                        </div>
                    </div>

                    <div class="col-xs-12 col-md-6">
                        <div>
                            <img src="<%= user.picture %>" alt="">
                        </div>
                    </div>
                </div>
            </div>
    </body>

<%- include('../part/foot') %>

</html>